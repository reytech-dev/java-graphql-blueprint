spring:
  r2dbc:
    url: r2dbc:postgresql://postgres:5432/backend
    username: backend
    password: backend
  flyway:
    url: jdbc:postgresql://postgres:5432/backend
    user: backend
    password: backend
  graphql:
    graphiql:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
